// Main module - imports from math.tjs

import { add, multiply } from './math.tjs'

export function calculate(x: 0, y: 0) -> 0 {
  // (x + y) * 2
  return multiply(add(x, y), 2)
}

test 'calculate combines add and multiply' {
  expect(calculate(3, 4)).toBe(14)  // (3 + 4) * 2 = 14
}
