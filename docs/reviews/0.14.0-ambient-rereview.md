# Pre-release triage: base 9245f13 → HEAD 810ab14 (minor, verify depth: fast)

**Verdict: GO_WITH_FOLLOWUPS.** No blockers. One minor finding (unverified) is deferred to TODO.md.

**STATUS: CLEARED** — 2026-09-25. The unit-row half of the follow-up is done rather than routed: `declare global`, `declare module 'x'`, `export declare namespace` and `declare abstract class` are pinned (`declare const enum` already was). Each emits nothing, and each was checked against what TypeScript emits — also nothing — so the rows pin correct behaviour, not merely current output. The corpus-fixture half stays open in `TODO.md` (a compat-scan fixture needs a new corpus entry, which is a larger change than belongs in the last commit before a publish).

## Per-lens summary

| Lens | Findings | Blockers | Notes |
|---|---|---|---|
| blast-radius | 1 minor (unverified) | 0 | The fix is correct and its exact shapes are pinned. The regression's corpus gap is still open. |
| correctness | none reported | 0 | The 9245f13 BLOCK (`declare interface` / `declare type` were erased) is resolved by 810ab14. |
| other lenses | not run at this depth | — | |

## Blockers

None.

## Minor

- **(unverified) The guard that missed the regression is still not hardened.** `TODO.md:4263`, lens: blast-radius.
  - compat-scan stayed green at 1973/1973 while ambient types were being erased, because none of the six corpora contains an ambient type.
  - The fix is pinned only by unit rows in `src/lang/from-ts-class-totality.test.ts`, and those cover the three exact shapes.
  - These ambient forms have no row:
    - `declare global`
    - `declare module 'x'`
    - `export declare namespace`
    - `declare abstract class`
    - `declare const enum`
  - The allowlist claims that unlisted forms "fall through to old behaviour", and no test checks that claim.
  - The lens probed these forms by hand, comparing c42a689 with HEAD, and found no behaviour change today.
  - It is not release-blocking. `fromTS` ships to consumers through `tjs-lang/lang/from-ts` and the browser from-ts bundle, and `.d.ts`-shaped input is common. So if the next edit here breaks one of these forms, a consumer would probably find it before any gate does.

## Follow-ups by destination

### This repo: `TODO.md`

- [ ] **(already filed, TODO.md ~line 4263)** Add a corpus fixture with ambient TYPE declarations. This is a small `.d.ts`-style module in the compat-scan fixtures, covering ambient interfaces, type aliases, functions, enums and namespaces. Keep the item open; do not treat it as done because it is written down.
- [ ] **(new, append to the same "0.14.0 ambient-statement review" section)** Add rows to `src/lang/from-ts-class-totality.test.ts` for the ambient forms that are not listed and for the ModuleDeclaration forms: `declare global`, `declare module 'x'`, `export declare namespace`, `declare abstract class` and `declare const enum`. Each row should assert today's byte output. This makes the allowlist's "unlisted forms fall through" claim testable. It is cheaper than the corpus fixture, so do it first, before or soon after 0.14.0. Verify the finding's hand-probe first (it is unverified).

### UPSTREAM.md + upstream repo

None. No ecosystem findings.

### Shared practices (`tosijs-coding-practices`) / CLAUDE.md

None. No practices findings.

## Failing or skipped tests

None were reported by the coverage findings in this run.

## Completeness gaps

- Verify depth was fast. The only finding is unverified and did not drive the verdict.
- No cycle flag. There is no blocker in this run, so the rule's conditions are not met. (The prior BLOCK in `docs/reviews/0.14.0-ambient-review.md` has been remediated, and this run found nothing blocking in that remediation.)
