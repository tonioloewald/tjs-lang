<!--{"hidden": true}-->

# TODO

This project's open work lives on its own board, not in this file (owner, 2026-09-26). The
board is https://virta.tosijs.net/host/#?virta.scope=tjs-lang. From a shell, `virta brief`
shows what is new, in progress and ready for this repo; `virta ls "project:tjs-lang
status:ready"` and `virta show #n` read it. File new work with `virta create "…" --project
tjs-lang`, with a body that says why and what done looks like.

Everything this file used to list — 583 items, 169 of them done — was imported onto the board
on 2026-09-26 at commit `dd34dc3`, together with the GitHub issues and `UPSTREAM.md`. Each
card's Source link points at the line it came from, at that commit, and `git log -p TODO.md`
keeps the full history. `TODO-ARCHIVE.md` still holds the older completed-work history.

Everything imported landed in the backlog, so nothing is "ready" until it is triaged on the
board. Some imported cards are commentary rather than work (the file mixed design notes with
tasks); close those as `not-a-task` when you meet them.

`UPSTREAM.md` stays a file: filing upstream and mirroring the finding there is the shared
practices' convention. New upstream findings go in both places — the file and a card.

(This file is prose on purpose: a list item here would be imported as a task.)
